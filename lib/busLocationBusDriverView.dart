import 'package:flutter/material.dart';

class BusLocationBusDriverView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Test Bus Driver Screen'),
      ),
    );
  }
}
